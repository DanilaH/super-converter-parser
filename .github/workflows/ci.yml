name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    # Draft PRs are implementation/review work in progress. Avoid consuming
    # private-repository hosted minutes on every intermediate commit; the full
    # Ubuntu + Windows gate runs when the PR is marked ready for review.
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    strategy:
      fail-fast: false
      matrix:
        # Node 20/npm 10 bundles node-gyp 10, which cannot discover VS 2026 on windows-latest.
        # Keep VS2022 until the Node/npm native-build toolchain is upgraded.
        os: [ubuntu-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
