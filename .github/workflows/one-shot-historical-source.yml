name: One-shot historical source compatibility

on:
  push:
    branches: [fix/historical-source-schema]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/historical-source-schema
          fetch-depth: 0
      - name: Apply read-only compatibility patch
        run: python .github/scripts/historical-source-patch.py
      - name: Remove temporary patch machinery
        run: rm .github/scripts/historical-source-patch.py .github/workflows/one-shot-historical-source.yml
      - name: Commit compatibility patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/db/store.ts src/db/store.test.ts README.md .github/scripts/historical-source-patch.py .github/workflows/one-shot-historical-source.yml
          git diff --cached --check
          git commit -m "fix(db): read historical discovery stores safely"
          git push origin HEAD:fix/historical-source-schema
