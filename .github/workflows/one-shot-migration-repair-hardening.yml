name: One-shot migration repair hardening

on:
  push:
    branches: [fix/migration-repair-hardening]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/migration-repair-hardening
          fetch-depth: 0

      - name: Apply migration repair patch
        run: python .github/apply-migration-repair-hardening.py

      - name: Remove temporary patch machinery
        run: rm .github/apply-migration-repair-hardening.py .github/workflows/one-shot-migration-repair-hardening.yml

      - name: Commit migration repair hardening
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/db/store.ts src/db/store.test.ts .github/apply-migration-repair-hardening.py .github/workflows/one-shot-migration-repair-hardening.yml
          git diff --cached --check
          git commit -m "fix(db): rerun idempotent schema repair on current stores"
          git push origin HEAD:fix/migration-repair-hardening
